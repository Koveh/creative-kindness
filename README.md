# Креативное добро

Платформа для публикации и управления статьями о креативных проектах социального значения.

## 🎨 Дизайн

Дизайн проекта доступен в Figma: [Креативное добро](https://www.figma.com/design/QRjYPtYEac28V4la2LLlh1/%D0%9A%D1%80%D0%B5%D0%B0%D1%82%D0%B8%D0%B2%D0%BD%D0%BE%D0%B5-%D0%B4%D0%BE%D0%B1%D1%80%D0%BE?node-id=312-759&t=MznTOtXmv97CRFRF-1)

## ⚡ Технологии

- **Frontend**: Next.js 15, React, TypeScript
- **UI**: shadcn/ui, Radix UI, Tailwind CSS
- **Шрифт**: Suisse International
- **База данных**: PostgreSQL
- **Хранилище файлов**: MinIO
- **Аутентификация**: Custom auth с localStorage

## 🚀 Быстрый старт

1. **Клонируйте репозиторий**
   ```bash
   git clone git@github.com:Koveh/creative-kindness.git
   cd creative-kindness
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   ```bash
   cp .env.example .env
   ```
   Заполните файл `.env` вашими данными для PostgreSQL и MinIO.

4. **Запустите проект**
   ```bash
   npm run dev
   ```

5. **Откройте в браузере**
   - Основной сайт: http://localhost:3000
   - Панель администратора: http://localhost:3000/admin

## 📁 Структура проекта

```
src/
├── app/
│   ├── admin/           # Панель администратора
│   ├── api/             # API маршруты
│   │   ├── auth/        # Аутентификация
│   │   ├── users/       # Управление пользователями
│   │   ├── articles/    # Управление статьями
│   │   └── upload/      # Загрузка изображений
│   ├── globals.css      # Глобальные стили
│   └── layout.tsx       # Основной layout
├── components/ui/       # UI компоненты (shadcn/ui)
├── lib/
│   ├── auth.ts         # Менеджер аутентификации
│   └── minio.ts        # MinIO клиент
└── public/
    ├── fonts/          # Шрифты Suisse International
    └── Vector.svg      # Логотип
```

## 🔧 Настройка

### База данных PostgreSQL

Создайте таблицы:

```sql
-- Таблица пользователей
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    phone VARCHAR(50),
    description TEXT,
    telegram VARCHAR(100),
    role VARCHAR(50) DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица статей
CREATE TABLE articles (
    id SERIAL PRIMARY KEY,
    title VARCHAR(500) NOT NULL,
    content TEXT,
    author_email VARCHAR(255) NOT NULL,
    writer VARCHAR(255),
    company VARCHAR(255),
    status VARCHAR(50) DEFAULT 'draft',
    views INTEGER DEFAULT 0,
    publish_date TIMESTAMP,
    description TEXT,
    title_image VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (author_email) REFERENCES users(email)
);
```

### MinIO

Создайте bucket для изображений:

```bash
mc mb your-minio/creative-kindness
mc anonymous set public your-minio/creative-kindness
```

## 👥 Администраторы по умолчанию

- **Даниил Ковех**: daniil@koveh.com
- **Федор Шубочкин**: doc.shuba@yandex.ru

## 📝 Функции

### Панель администратора
- ✅ Аутентификация с сохранением сессии (6 месяцев)
- ✅ Управление пользователями
- ✅ Управление статьями
- ✅ Загрузка изображений в MinIO
- ✅ Поддержка Markdown для контента
- ✅ Статусы публикации (черновик/рецензия/опубликовано)

### Статьи
- ✅ Поддержка Markdown
- ✅ Изображения заголовков
- ✅ Метаданные (автор, компания, описание)
- ✅ Счетчик просмотров
- ✅ Связь с автором по email

## 🎨 Дизайн-система

- **Шрифт**: Suisse International (Regular, Medium, Bold)
- **Компоненты**: shadcn/ui + Radix UI
- **Язык интерфейса**: Русский
- **Иконка**: Кастомный Vector.svg

## 📄 Лицензия

Частный проект

## 👨‍💻 Разработка

Разработано с использованием современных технологий для создания быстрой и удобной платформы управления контентом.
